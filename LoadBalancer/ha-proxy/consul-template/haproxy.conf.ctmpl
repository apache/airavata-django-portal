frontend http-in
 bind *:80
 default_backend www

backend www{{range service "Portal"}}
 server {{.ID}} {{.Address}}:{{.Port}}{{end}}


listen stats *:1936
    stats enable
    stats uri /
    stats hide-version
    #stats auth someuser:password
